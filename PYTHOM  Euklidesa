def euclidean_algorithm(a, b):
    while b: a, b = b, a % b 
    return a
a = int(input("Podaj liczbę: ")) 
b = int(input("Podaj liczbę: "))
nwd = euclidean_algorithm(a, b) 
print("Największy wspolny dzielnik:", nwd)